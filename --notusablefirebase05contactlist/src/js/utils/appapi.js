// import * as Firebase from 'firebase';
let Firebase = require('firebase');
import AppActions from './../actions/appactions';

export default {
	saveContact(contact){
		console.log('saving from API');
		// this.firebaseRef = new Firebase('https://contactlist-84c6b.firebaseio.com/contacts');
		// this.firebaseRef.push({
		// 	contact: contact
		// });
	}
}